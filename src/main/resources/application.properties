spring.application.name=VocabApp
server.port=8080
# spring.datasource.url=jdbc:mariadb://h7xe2knj2qb6kxal.cbetxkdyhwsb.us-east-1.rds.amazonaws.com:3306/qcw4r0odq4lx4kbt?sslMode=trust
# spring.datasource.username=t6cubam77pe7ivnc
# spring.datasource.password=nq4jdvb0b5tpufbo
# spring.datasource.driver-class-name=org.mariadb.jdbc.Driver



# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
# # TEMP ONLY for local testing
# spring.security.user.name=admin
# spring.security.user.password=admin123
# spring.security.user.roles=USER


# ------------------------------------------------------------
# Default Local Database (H2)
# ------------------------------------------------------------
# Use an in-memory H2 DB for local development — no credentials needed
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
# ------------------------------------------------------------
# Logging
# ------------------------------------------------------------
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=warn

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Optional: show SQL for debugging
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none  # VERY IMPORTANT! Let Flyway manage schema

# ------------------------------------------------------------
# Security (optional temporary local user)
# ------------------------------------------------------------
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=USERok